/**
 * colors.ts — LEGACY `colors` obyekti (36 ekran ishlatadi).
 * Qiymatlar tokens.ts'dan keladi (yagona manba). Eski kalitlar o'zgarmagan.
 */
import { tokens } from './tokens';

export const colors = {
  disabledButtonColor: tokens.color.muted,
  placeHolderColor: tokens.color.placeholder,
  statusBarColor: tokens.color.primaryDark,
  green: tokens.color.success,
  backgroundColor: tokens.color.background,
  blue: tokens.color.primary,
  white: tokens.color.white,
  black: tokens.color.black,
  red: tokens.color.red,
};

import {StyleSheet} from 'react-native';
import {style} from './style';

export default StyleSheet.create({
  backimage: {
    position: 'absolute',
    height: '40%',
    width: '100%',
  },
  marginTop: {
    marginTop: 10,
  },
});
